# Copy to .env and update values before docker compose up
# Backend settings
FOREMAN_DATASETS_DIR=/app/datasets
CORS_ALLOW_ORIGINS=*

# Override ports if desired
# FRONTEND_HOST_PORT=80
# BACKEND_HOST_PORT=8000
