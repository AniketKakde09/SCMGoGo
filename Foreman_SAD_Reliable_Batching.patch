--- a/backend/sad_workflow.py
+++ b/backend/sad_workflow.py
@@ -1,6 +1,10 @@
 """SAD proposal generation. Never writes to the source workbook or Jira."""
 from __future__ import annotations
 import io
+import json
+import logging
+import os
+import time
 import re
 import uuid
 from difflib import SequenceMatcher
@@ -55,7 +59,9 @@
         else:
             current = {'id': 'section-1', 'title': 'Overview', 'text': line + '\n'}
             sections.append(current)
-    return sections[:80]
+    if len(sections) > 80:
+        raise HTTPException(413, 'SAD has more than 80 sections; split it into separate documents')
+    return sections
 
 
 def _tokens(text):
@@ -78,6 +84,101 @@
     return sorted(found, key=lambda item: item['score'], reverse=True)[:3]
 
 
+logger = logging.getLogger(__name__)
+
+# Character budgets are conservative approximations, not model token guarantees.
+CHUNK_CHARS = 2600
+MAX_CHUNKS = 60
+MAX_TICKETS = 60
+BATCH_OUTPUT_TOKENS = 3500
+
+
+def _chunks(sections):
+    """Never silently truncate SAD content; retain each source section ID."""
+    chunks = []
+    for section in sections:
+        content = section['text'].strip()
+        if not content:
+            continue
+        # Split on word boundaries, including when a single paragraph is huge.
+        words = content.split()
+        part = []
+        size = 0
+        for word in words:
+            if len(word) > CHUNK_CHARS:
+                raise HTTPException(422, f"Section {section['id']} contains an oversized unbroken token")
+            if part and size + len(word) + 1 > CHUNK_CHARS:
+                chunks.append({'id': section['id'], 'title': section['title'], 'text': ' '.join(part)})
+                part, size = [], 0
+            part.append(word)
+            size += len(word) + 1
+        if part:
+            chunks.append({'id': section['id'], 'title': section['title'], 'text': ' '.join(part)})
+    if len(chunks) > MAX_CHUNKS:
+        raise HTTPException(413, f'SAD requires {len(chunks)} batches (maximum {MAX_CHUNKS}); process as separate documents')
+    return chunks
+
+
+def _generate_batch(llm, chunk, index, total):
+    prompt = (
+        'Convert ONLY the supplied SAD excerpt into a SMALL draft delivery hierarchy. '
+        'Return ONLY a JSON object with key tickets, an array of AT MOST THREE objects. '
+        'If there is no actionable requirement return {"tickets":[]}. '
+        'For actionable content return one Epic, one child Feature and optionally one child Story OR Task. '
+        'Each ticket must have temp_id, parent_temp_id (empty for Epic), type, title, '
+        'description (maximum 160 characters), acceptance_criteria (at most 2 short strings), '
+        'source_section_id, source_excerpt (exact short substring from excerpt), assumptions (at most 1 short string). '
+        'Use only the provided source_section_id. Use IDs e1, f1, s1 within this batch. '
+        'Hierarchy Epic -> Feature -> Story/Task. Do not invent requirements or Jira keys. '
+        'Do not add generic security, testing or deployment work unless explicitly supported. '
+        'Keep the entire JSON short and complete; no markdown.'
+    )
+    payload = json.dumps({'source_section_id': chunk['id'], 'section_title': chunk['title'],
+                          'excerpt': chunk['text']}, ensure_ascii=False)
+    last_error = None
+    for attempt in range(2):
+        try:
+            result = llm.generate_json(prompt, payload, max_tokens=BATCH_OUTPUT_TOKENS)
+            tickets = result.get('tickets')
+            if not isinstance(tickets, list) or len(tickets) > 3:
+                raise ValueError('Expected tickets array containing at most 3 items')
+            if tickets:
+                _validate_batch(tickets, chunk)
+            logger.info('SAD batch %s/%s generated %s tickets', index, total, len(tickets))
+            return tickets
+        except Exception as exc:
+            last_error = exc
+            logger.warning('SAD batch %s/%s attempt %s failed: %s', index, total, attempt + 1, type(exc).__name__)
+            if attempt == 0:
+                time.sleep(0.5)
+    raise HTTPException(502, f'SAD batch {index}/{total} failed after retry ({type(last_error).__name__}). No partial proposal was returned.') from last_error
+
+
+def _validate_batch(tickets, chunk):
+    by_id = {}
+    for item in tickets:
+        if not isinstance(item, dict) or item.get('type') not in TYPES or not str(item.get('title', '')).strip():
+            raise ValueError('Ticket has invalid type or title')
+        key = str(item.get('temp_id', '')).strip()
+        if not key or key in by_id:
+            raise ValueError('Missing or duplicate ticket temp_id')
+        by_id[key] = item
+        if item.get('source_section_id') != chunk['id']:
+            raise ValueError('Ticket source section does not match input')
+        excerpt = str(item.get('source_excerpt', '')).strip()
+        if not excerpt or excerpt not in chunk['text']:
+            raise ValueError('Source excerpt is not an exact substring of input')
+    expected = {'Feature': 'Epic', 'Story': 'Feature', 'Task': 'Feature'}
+    for item in tickets:
+        parent_id = str(item.get('parent_temp_id') or '').strip()
+        parent = by_id.get(parent_id)
+        if item['type'] == 'Epic':
+            if parent_id:
+                raise ValueError('Epic must not have a parent')
+        elif not parent or parent['type'] != expected[item['type']]:
+            raise ValueError('Ticket has invalid parent hierarchy')
+
+
 def generate_proposal(text: str, excel_path: Path, document_title: str):
     text = text.strip()
     if len(text) < 60:
@@ -85,52 +186,50 @@
     if len(text) > MAX_CHARS:
         raise HTTPException(413, f'SAD text must be {MAX_CHARS} characters or fewer')
     sections = sections_from_text(text)
+    chunks = _chunks(sections)
+    if not chunks:
+        raise HTTPException(422, 'No readable SAD section content found')
     llm = LLMClient()
     if not llm.enabled:
         raise HTTPException(503, 'SAD generation requires LLM_PROVIDER=groq or bedrock. No tickets were generated.')
-    prompt = ('You convert a system architecture document into delivery work. Return ONLY JSON with key tickets: array of objects. '
-              'Each object has temp_id, parent_temp_id (empty for Epic), type (Epic, Feature, Story, Task), title, description, acceptance_criteria (array of strings), source_section_id, source_excerpt (verbatim short quote), assumptions (array). '
-              'Hierarchy Epic -> Feature -> Story/Task. Cover application, security, infrastructure, QA, observability and release when actually supported by the SAD. '
-              'Never invent requirements; mark uncertainty in assumptions. Do not create Jira IDs. Do not claim existing backlog is absent. '
-              'Keep source_section_id from supplied sections and ensure unique temp_id values. Max 60 tickets.')
+
+    # Stage each batch in memory. On any error, return no proposal; never publish to Jira.
+    staged = []
+    for index, chunk in enumerate(chunks, start=1):
+        raw = _generate_batch(llm, chunk, index, len(chunks))
+        if len(staged) + len(raw) > MAX_TICKETS:
+            raise HTTPException(422, f'SAD would exceed {MAX_TICKETS} draft tickets; split document into smaller SADs. No partial proposal was returned.')
+        local_ids = {str(item['temp_id']).strip(): f'sad-{uuid.uuid4().hex[:12]}' for item in raw}
+        for item in raw:
+            parent = str(item.get('parent_temp_id') or '').strip()
+            criteria = item.get('acceptance_criteria', [])
+            assumptions = item.get('assumptions', [])
+            staged.append({
+                'id': local_ids[str(item['temp_id']).strip()],
+                'parent_id': local_ids.get(parent, ''),
+                'type': item['type'], 'title': str(item['title']).strip()[:255],
+                'description': str(item.get('description', '')).strip(),
+                'acceptance_criteria': [str(x) for x in criteria] if isinstance(criteria, list) else [],
+                'source_section_id': chunk['id'],
+                'source_excerpt': str(item['source_excerpt']).strip()[:500],
+                'assumptions': [str(x) for x in assumptions] if isinstance(assumptions, list) else [],
+            })
+    if not staged:
+        raise HTTPException(422, 'No actionable requirements found in the SAD; no tickets were generated')
     try:
-        output = llm.generate_json(prompt, str({'document_title':document_title, 'sections':sections})[:MAX_CHARS], max_tokens=7000)
+        backlog = pd.read_excel(excel_path, sheet_name='Backlog').fillna('')
     except Exception as exc:
-        raise HTTPException(502, f'SAD generation provider failed: {exc}')
-    raw = output.get('tickets')
-    if not isinstance(raw, list) or not raw:
-        raise HTTPException(502, 'Model did not return a valid tickets array; please retry or revise the document')
-    by_source = {s['id']:s for s in sections}
-    prepared = []
-    id_map = {}
-    for item in raw[:60]:
-        if not isinstance(item, dict) or item.get('type') not in TYPES or not str(item.get('title','')).strip():
-            continue
-        original = str(item.get('temp_id','')).strip()
-        if not original or original in id_map:
-            raise HTTPException(502, 'Generated ticket IDs are missing or duplicated')
-        id_map[original] = f'sad-{uuid.uuid4().hex[:12]}'
-    for item in raw[:60]:
-        if not isinstance(item, dict) or str(item.get('temp_id','')) not in id_map:
-            continue
-        kind = item['type']
-        parent = str(item.get('parent_temp_id') or '').strip()
-        if kind != 'Epic' and parent not in id_map:
-            raise HTTPException(502, f'Generated {kind} has no valid parent')
-        if kind == 'Epic' and parent:
-            raise HTTPException(502, 'Generated Epic unexpectedly has a parent')
-        source_id = str(item.get('source_section_id',''))
-        if source_id not in by_source:
-            raise HTTPException(502, 'Generated ticket has an unknown SAD source section')
-        criteria = item.get('acceptance_criteria', [])
-        prepared.append({'id':id_map[str(item['temp_id'])], 'parent_id':id_map.get(parent,''), 'type':kind, 'title':str(item['title']).strip()[:255], 'description':str(item.get('description','')).strip(), 'acceptance_criteria':[str(x) for x in criteria] if isinstance(criteria,list) else [], 'source_section_id':source_id, 'source_excerpt':str(item.get('source_excerpt',''))[:500], 'assumptions':item.get('assumptions',[]) if isinstance(item.get('assumptions',[]),list) else []})
-    types_by_id = {item['id']:item['type'] for item in prepared}
-    for item in prepared:
-        expected = {'Feature':'Epic','Story':'Feature','Task':'Feature'}.get(item['type'])
-        if expected and types_by_id.get(item['parent_id']) != expected:
-            raise HTTPException(502, f"Generated {item['type']} has invalid parent hierarchy")
-    backlog = pd.read_excel(excel_path, sheet_name='Backlog').fillna('')
-    for item in prepared:
+        logger.exception('Unable to read SAD duplicate-check dataset')
+        raise HTTPException(422, 'Unable to read the dataset Backlog sheet for duplicate screening') from exc
+    for item in staged:
         item['duplicate_candidates'] = _candidate(item, backlog)
         item['review_status'] = 'potential_duplicate' if item['duplicate_candidates'] else 'new_proposal'
-    return {'document_title':document_title, 'sections':[{'id':s['id'],'title':s['title']} for s in sections], 'tickets':prepared, 'coverage': [{'section_id':s['id'],'title':s['title'],'ticket_count':sum(t['source_section_id']==s['id'] for t in prepared)} for s in sections], 'duplicate_method':'Lexical/title candidate screening; human review required. No semantic verification claimed.', 'publish_status':'draft_only'}
+    return {
+        'document_title': document_title,
+        'sections': [{'id': s['id'], 'title': s['title']} for s in sections],
+        'tickets': staged,
+        'coverage': [{'section_id': s['id'], 'title': s['title'],
+                      'ticket_count': sum(t['source_section_id'] == s['id'] for t in staged)} for s in sections],
+        'duplicate_method': 'Lexical/title candidate screening; human review required. No semantic verification claimed.',
+        'publish_status': 'draft_only',
+    }
